#include <iostream>
#include <algorithm>
#include <vector>
#include <unordered_map>
using namespace std;

struct NODE
{
    int Rank;
    string School;
    int TWS = 0;
    int NS;
};

int main()
{
    int num;
    cin >> num;
    unordered_map<NODE> temp;
    int size = 0;
    for (int i = 0; i < num; i++)
    {
        string stuname, schname;
        int score;
        cin >> stuname >> score >> schname;

        if(stuname[0] == 'T')
            data[]

    }
}